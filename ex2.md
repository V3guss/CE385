โค้ดสร้างตัวแปรข้อมูล 6 ชนิด ได้แก่ string, number, boolean, undefined, null และ array
ใช้ typeof เพื่อตรวจสอบชนิดของข้อมูลแต่ละตัว
นอกจากนี้ยังใช้ Number() แปลงข้อความเป็นตัวเลข และ Number.isNaN() ตรวจสอบค่า NaN